<Window x:Class="LotReport.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LotReport.Views"
        xmlns:y="clr-namespace:LotReport.Views.MainWindowControls"
        xmlns:r="clr-namespace:LotReport.Views.ReusableControls"
        xmlns:viewModels="clr-namespace:LotReport.ViewModels"
        mc:Ignorable="d"
        Title="Lot Report" Height="720" Width="1280"
        WindowState="Maximized"
        Loaded="Window_Loaded">
    <Window.DataContext>
        <viewModels:MainWindowViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Menu Grid.Row="0">
            <MenuItem Header="_Tools">
                <MenuItem Header="_Refresh Lot(s)" Command="{Binding RefreshLotsCommand}"/>
                <MenuItem Header="_Settings" Command="{Binding SettingsCommand}"/>
            </MenuItem>
        </Menu>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.2*"/>
                <ColumnDefinition Width="0.8*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="1*"/>
                    <RowDefinition Height="1.5*"/>
                </Grid.RowDefinitions>

                <y:LotListControl Grid.Row="0"/>
                <y:LotControl Grid.Row="1"/>
            </Grid>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid x:Name="leadFrameTableGrid" Grid.Row="0" Background="#263238">
                    <r:LeadFrameMapControl
                        HorizontalContentAlignment="Center"
                        VerticalContentAlignment="Center"
                        DoubleClickCell="{Binding DieCommand}"
                        LeadFrameTable="{Binding LeadFrameMap}"
                        GridMaxWidth="{Binding ElementName=leadFrameTableGrid, Path=ActualWidth}"
                        GridMaxHeight="{Binding ElementName=leadFrameTableGrid, Path=ActualHeight}"
                        LotId="{Binding LeadFrameMap.LotId}"
                        MagazineId="{Binding LeadFrameMap.MagazineId}"
                        LeadFrameId="{Binding LeadFrameMap.LeadFrameId}"/>
                </Grid>

                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <GroupBox Header="Lot Info" Margin="5">
                        <StackPanel Grid.Column="0" Margin="3">
                            <r:LabelTextBox Label="Machine ID:" Value="{Binding LotData.MachineId}" ReadOnly="True" Margin="3"/>
                            <r:LabelTextBox Label="Lot ID:" Value="{Binding LotData.LotId}" ReadOnly="True" Margin="3"/>
                            <r:LabelTextBox Label="Operator ID:" Value="{Binding LotData.OperatorId}" ReadOnly="True" Margin="3"/>
                            <r:LabelTextBox Label="Product Code:" Value="{Binding LotData.ProductCode}" ReadOnly="True" Margin="3"/>
                            <r:LabelTextBox Label="Bonding Diagram:" Value="{Binding LotData.BondingDiagram}" ReadOnly="True" Margin="3"/>
                            <r:LabelTextBox Label="Recipe:" Value="{Binding LotData.RecipeName}" ReadOnly="True" Margin="3"/>
                            <r:LabelTextBox Label="Start DateTime:" Value="{Binding LotData.StartTime}" ReadOnly="True" Margin="3"/>
                            <r:LabelTextBox Label="End DateTime:" Value="{Binding LotData.EndTime}" ReadOnly="True" Margin="3"/>
                        </StackPanel>
                    </GroupBox>
                </Grid>
            </Grid>
        </Grid>

        <StatusBar Grid.Row="2" Background="#2962FF" Height="20">
            <StatusBarItem Content="{Binding Status}" TextBlock.Foreground="White"/>
        </StatusBar>
    </Grid>
</Window>
